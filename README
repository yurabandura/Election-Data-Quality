Single web page with interactive map showing election, demographic data and boundaries of voting precincts and congressional districts in New York, Kentucky and Pennsylvania.
Allows user to reshape boundaries, fix errors determined during preprocessing. 
User Interface written in JavaScript, styled with MaterializeCSS, Leaflet module used as mapping library. 
REST server implemented in Java with Spring Web Services. Communication with database(MySQL) performed via JPA.
